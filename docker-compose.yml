version: "3.9"

services:
  api:
    build: .
    ports:
      - "8070:8070"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8070/health"]
      interval: 30s
      timeout: 5s
      retries: 3
